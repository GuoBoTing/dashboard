# ============================================
# Flambé Dashboard - 環境變數範本
# ============================================
#
# 使用說明：
# 1. 複製此檔案並重新命名為 .env
# 2. 填入你的實際 API 金鑰和設定值
# 3. 絕對不要將 .env 提交到 Git！
#
# 指令：cp .env.example .env
# ============================================

# --------------------------------------------
# Meta (Facebook) API 設定
# --------------------------------------------
# ⚠️ 重要：不要在這裡存放短期 Access Token！
# 短期 Token 會在 1-2 小時後過期
#
# 正確做法：
# 1. 取得短期 token：https://developers.facebook.com/tools/explorer/
# 2. 在 Streamlit UI 的 "Token 管理" 區塊，使用短期 token 生成長期 token
# 3. 長期 token 會自動存儲在 session state，並在快過期時自動刷新
# --------------------------------------------

# Meta App ID（應用程式 ID）- 必填
# 在 Meta 開發者平台 > 你的應用程式 > 設定 > 基本資料中取得
# 範例：123456789012345
META_APP_ID=your_app_id_here

# Meta App Secret（應用程式密鑰）- 必填
# 在 Meta 開發者平台 > 你的應用程式 > 設定 > 基本資料中取得
# ⚠️ 絕對不要公開此密鑰！
META_APP_SECRET=your_app_secret_here

# Meta 廣告帳號 ID - 必填
# 格式必須包含 act_ 前綴
# 範例：act_123456789
# 在 Meta 廣告管理員 > 帳號設定中取得
META_ACCOUNT_ID=act_your_account_id_here

# Meta Long-lived Token（長期存取權杖）- 選填
# 如果留空，首次使用時需要在 UI 中用短期 token 生成
# 長期 token 有效期約 60 天，系統會在剩餘 7 天時自動刷新
# 如果你已經有 long-lived token，可以填在這裡
META_LONG_LIVED_TOKEN=

# Meta OAuth Redirect URI - OAuth 2.0 認證用（推薦）
# 本地開發：http://localhost:8501
# 生產環境：https://your-app-name.streamlit.app
# ⚠️ 必須在 Meta 開發者平台的 OAuth 設定中新增此 URI
META_OAUTH_REDIRECT_URI=http://localhost:8501

# --------------------------------------------
# WooCommerce API 設定
# --------------------------------------------
# 你的 WooCommerce 商店網址（包含 https://）
# 範例：https://your-store.com
WC_URL=https://your-store.com

# WooCommerce Consumer Key
# 在 WooCommerce > 設定 > 進階 > REST API 中生成
WC_CONSUMER_KEY=ck_your_consumer_key_here

# WooCommerce Consumer Secret
# 在 WooCommerce > 設定 > 進階 > REST API 中生成
WC_CONSUMER_SECRET=cs_your_consumer_secret_here

# --------------------------------------------
# 應用程式設定（選填）
# --------------------------------------------
# 預設進貨成本率（0-100 的數字，預設 50）
DEFAULT_COGS_RATE=50

# 營業稅率（0-100 的數字，預設 5）
TAX_RATE=5

# 除錯模式（true/false）
DEBUG_MODE=false

# --------------------------------------------
# 部署環境（選填）
# --------------------------------------------
# 環境類型：development, staging, production
ENVIRONMENT=development

# ============================================
# 安全提醒
# ============================================
# ⚠️  請確保 .env 已加入 .gitignore
# ⚠️  不要在公開場合分享 API 金鑰
# ⚠️  定期輪換 Token 和密鑰
# ⚠️  在生產環境使用 Streamlit Secrets 更安全
# ============================================